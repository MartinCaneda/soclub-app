/* export default function FavoriteButton() {

  return (
    <button className="favButton" onClick={}>
      <span>
        {EventIsFavorite?.isFavorite ? "❤️" : <i>Add to Favorites</i>}
      </span>
    </button>
  );
}
 */
